[package]
name = "winbend-composite"
version = "0.1.0"
edition = "2021"
description = "Tracks the black laptop screen in a clip and projects WinBend fold frames onto it."
publish = false

[dependencies]
image = { version = "0.25", default-features = false, features = ["png"] }

[profile.release]
opt-level = 3
